git_url("git@github.com:RevenueCat/apple-certificates-fastlane-match.git")

storage_mode("git")

type("development") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["com.revenuecat.XcodeDirectIntegration"])
username(ENV["FASTLANE_USER"])

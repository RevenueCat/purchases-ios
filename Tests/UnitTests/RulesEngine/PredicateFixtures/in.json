{
  "fixtures": [
    {
      "id": "in_substring_match_for_string_haystack",
      "predicate": { "in": ["bar", "foobar"] },
      "variables": {},
      "expected": true
    },
    {
      "id": "in_substring_no_match",
      "predicate": { "in": ["baz", "foobar"] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_array_membership_strict_types",
      "predicate": { "in": ["US", ["US", "CA", "MX"]] },
      "variables": {},
      "expected": true
    },
    {
      "id": "in_array_membership_is_strict",
      "description": "Array.prototype.indexOf is strict (===), so 5 is not in [\"5\"]",
      "predicate": { "in": [5, ["5", "6"]] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_string_haystack_coerces_non_string_needle",
      "description": "String.prototype.indexOf coerces a non-string needle to a string, so 5 is found in \"12345\"",
      "predicate": { "in": [5, "12345"] },
      "variables": {},
      "expected": true
    },
    {
      "id": "in_unsupported_haystack_null",
      "description": "no indexOf in JS, so unsupported haystack types return false",
      "predicate": { "in": ["x", null] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_unsupported_haystack_number",
      "description": "no indexOf in JS, so unsupported haystack types return false",
      "predicate": { "in": ["x", 123] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_unsupported_haystack_bool",
      "description": "no indexOf in JS, so unsupported haystack types return false",
      "predicate": { "in": ["x", true] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_unsupported_haystack_object",
      "description": "no indexOf in JS, so unsupported haystack types return false",
      "predicate": { "in": ["x", {}] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_zero_args",
      "description": "json-logic-js declares in as function(a, b); a missing haystack short-circuits to false",
      "predicate": { "in": [] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_one_arg",
      "description": "a missing needle becomes JS undefined, which indexOf won't find",
      "predicate": { "in": ["only-one"] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_string_haystack_does_not_match_a_different_spelling",
      "description": "The needle is a precomposed e-acute é (U+00E9); the haystack spells it decomposed, as x, e (U+0065), a combining acute accent ◌́ (U+0301), y. indexOf searches code units, so the precomposed needle is absent",
      "predicate": { "in": ["\u00e9", "xe\u0301y"] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_array_haystack_does_not_match_a_different_spelling",
      "description": "The same two spellings of e-acute, now as needle and sole array element, where the search compares with === instead",
      "predicate": { "in": ["\u00e9", ["e\u0301"]] },
      "variables": {},
      "expected": false
    },
    {
      "id": "in_empty_needle_matches_a_non_empty_string_haystack",
      "description": "JS 'abcd'.indexOf('') is 0, so the operator matches",
      "predicate": { "in": ["", "abcd"] },
      "variables": {},
      "expected": true
    },
    {
      "id": "in_astral_needle_matches_a_string_haystack",
      "description": "The needle is the grinning face emoji 😀 (U+1F600, encoded as the surrogate pair U+D83D U+DE00) and the haystack holds it between a and b, so both code units match in order",
      "predicate": { "in": ["\ud83d\ude00", "a\ud83d\ude00b"] },
      "variables": {},
      "expected": true
    }
  ]
}

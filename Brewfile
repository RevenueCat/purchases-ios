# This list of dependencies can be installed using `brew bundle`.
# We're not actually running this on the CI, but it's solely used as a list of dependencies we use for caching purposes.
# The dependencies themselves are installed using the `install-brew-dependency` command in the .circleci/config.yml file.

tap "xcodesorg/made"

brew "swiftlint"
brew "xcbeautify"
brew "xcodes"
brew "mint"
